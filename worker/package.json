{
  "name": "form-of-learning-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:create": "wrangler d1 create form-of-learning",
    "db:migrate": "wrangler d1 migrations apply form-of-learning",
    "db:migrate:local": "wrangler d1 migrations apply form-of-learning --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
